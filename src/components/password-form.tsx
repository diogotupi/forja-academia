"use client";import{FormEvent,useState}from"react";const basePath=process.env.NEXT_PUBLIC_BASE_PATH??"/liderflix";
export function PasswordForm({token}:{token?:string}){const[message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget).entries());const path=token?'reset-password':'forgot-password';const response=await fetch(`${basePath}/api/auth/${path}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(token?{token,password:data.password}:{email:data.email})});const result=await response.json();setMessage(response.ok?(result.message??'Senha atualizada. Você já pode entrar.'):result.error)}return <form className="auth-form" onSubmit={submit}>{token?<label>NOVA SENHA<input name="password" type="password" minLength={10} required/></label>:<label>EMAIL<input name="email" type="email" required/></label>}<button className="button red">{token?'REDEFINIR SENHA':'ENVIAR INSTRUÇÕES'}</button>{message&&<p className="form-error">{message}</p>}</form>}
